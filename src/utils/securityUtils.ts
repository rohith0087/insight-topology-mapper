
// Legacy re-export file for backward compatibility
// This ensures all existing imports continue to work
export {
  securityPatterns,
  sanitizeInput,
  validateInput,
  secureHash,
  generateSecureToken,
  RateLimiter
} from './security';
